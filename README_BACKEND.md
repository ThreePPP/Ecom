# E-commerce Full Stack Application

## 🚀 ระบบ E-commerce พร้อม Backend แบบเต็มรูปแบบ

### ⚙️ Tech Stack

**Frontend:**
- Next.js 15
- React 19
- TypeScript
- Tailwind CSS

**Backend:**
- Node.js
- Express
- MongoDB
- Mongoose
- JWT Authentication
- bcryptjs

---

## 📋 ข้อกำหนดเบื้องต้น

ก่อนเริ่มต้น ต้องติดตั้งโปรแกรมเหล่านี้:

1. **Node.js** (v18 หรือสูงกว่า)
2. **MongoDB** (ติดตั้งบนเครื่อง หรือใช้ MongoDB Atlas)

### ติดตั้ง MongoDB บน Windows

1. ดาวน์โหลด MongoDB Community Server จาก: https://www.mongodb.com/try/download/community
2. ติดตั้งและรัน MongoDB
3. MongoDB จะรันที่ `mongodb://localhost:27017`

---

## 🛠️ การติดตั้งและเริ่มใช้งาน

### 1. ติดตั้ง Dependencies

```bash
npm install
```

### 2. ตั้งค่า Environment Variables

ไฟล์ `.env` และ `.env.local` ถูกสร้างไว้แล้ว คุณสามารถแก้ไขได้ตามต้องการ

**Backend (.env):**
```env
MONGODB_URI=mongodb://localhost:27017/ecommerce
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
```

**Frontend (.env.local):**
```env
NEXT_PUBLIC_API_URL=http://localhost:5000/api
```

### 3. เริ่มต้น MongoDB

ตรวจสอบว่า MongoDB กำลังรันอยู่:

```bash
# Windows
net start MongoDB

# หรือรันผ่าน MongoDB Compass
```

### 4. เพิ่มข้อมูลตัวอย่าง (Seed Database)

```bash
npm run seed
```

คำสั่งนี้จะสร้างสินค้าตัวอย่าง 5 รายการในฐานข้อมูล

### 5. เริ่มต้น Backend Server

เปิด Terminal หน้าต่างแรก:

```bash
npm run server
```

Backend จะรันที่ `http://localhost:5000`

### 6. เริ่มต้น Frontend Development Server

เปิด Terminal หน้าต่างที่สอง:

```bash
npm run dev
```

Frontend จะรันที่ `http://localhost:3000`

---

## 📁 โครงสร้างโปรเจค

```
ecommerce/
├── server/                      # Backend (Node.js + Express)
│   ├── src/
│   │   ├── config/             # การตั้งค่าฐานข้อมูล
│   │   ├── controllers/        # Business Logic
│   │   ├── middleware/         # Authentication Middleware
│   │   ├── models/             # MongoDB Models
│   │   ├── routes/             # API Routes
│   │   ├── utils/              # Utilities & Seeders
│   │   └── server.ts           # Entry Point
│   └── tsconfig.json
├── src/
│   └── app/
│       ├── component/          # React Components
│       ├── context/            # React Context
│       ├── lib/                # API Functions
│       └── util/               # Type Definitions
├── .env                        # Backend Environment Variables
├── .env.local                  # Frontend Environment Variables
└── package.json
```

---

## 🔑 ฟีเจอร์หลัก

### ✅ Authentication & Authorization
- ✨ สมัครสมาชิก (Register)
- ✨ เข้าสู่ระบบ (Login) 
- ✨ JWT Token Authentication
- ✨ Password Hashing ด้วย bcryptjs
- ✨ Role-based Access Control (User, Admin)

### ✅ Product Management
- ✨ ดูรายการสินค้าทั้งหมด
- ✨ กรองสินค้าตามหมวดหมู่
- ✨ ค้นหาสินค้า
- ✨ สินค้าแนะนำ (Featured)
- ✨ Flash Sale
- ✨ เพิ่ม/แก้ไข/ลบสินค้า (Admin)

### ✅ Shopping Cart
- ✨ เพิ่มสินค้าลงตะกร้า
- ✨ แก้ไขจำนวนสินค้า
- ✨ ลบสินค้าออกจากตะกร้า
- ✨ ล้างตะกร้า
- ✨ ซิงค์ข้อมูลกับ Backend

### ✅ Order Management
- ✨ สร้างคำสั่งซื้อ
- ✨ ดูประวัติการสั่งซื้อ
- ✨ ติดตามสถานะออเดอร์
- ✨ จัดการออเดอร์ (Admin)
- ✨ ระบบการจัดการสต็อกสินค้าอัตโนมัติ

---

## 🌐 API Endpoints

ดูรายละเอียดทั้งหมดได้ที่ [`API_DOCS.md`](./API_DOCS.md)

**ตัวอย่าง:**

- `POST /api/auth/register` - สมัครสมาชิก
- `POST /api/auth/login` - เข้าสู่ระบบ
- `GET /api/products` - ดูสินค้าทั้งหมด
- `POST /api/cart` - เพิ่มสินค้าลงตะกร้า
- `POST /api/orders` - สร้างออเดอร์

---

## 🧪 การทดสอบ API

### ใช้ Browser
เปิด `http://localhost:5000/api/health` เพื่อตรวจสอบว่า Backend ทำงาน

### ใช้ Postman หรือ Thunder Client

**ตัวอย่างการ Register:**

```
POST http://localhost:5000/api/auth/register
Content-Type: application/json

{
  "firstName": "สมชาย",
  "lastName": "ใจดี",
  "email": "somchai@example.com",
  "password": "password123",
  "phoneNumber": "0812345678",
  "dateOfBirth": "1990-01-01"
}
```

**ตัวอย่างการ Login:**

```
POST http://localhost:5000/api/auth/login
Content-Type: application/json

{
  "email": "somchai@example.com",
  "password": "password123"
}
```

---

## 📝 Scripts ที่มีให้ใช้

```bash
# Frontend
npm run dev          # รัน Next.js development server
npm run build        # Build production
npm run start        # รัน production server

# Backend
npm run server       # รัน Backend development server
npm run server:build # Build Backend TypeScript
npm run server:start # รัน Backend production

# Database
npm run seed         # เพิ่มข้อมูลตัวอย่าง
```

---

## 🔒 Security

- รหัสผ่านถูกเข้ารหัสด้วย **bcryptjs**
- ใช้ **JWT** สำหรับ Authentication
- Validation ทุก Input
- CORS Protection
- Environment Variables สำหรับข้อมูลสำคัญ

---

## 🎨 การใช้งานระบบ

### 1. สมัครสมาชิก
1. คลิกปุ่ม "เข้าสู่ระบบ" บน Navbar
2. คลิก "สมัครสมาชิกเลย"
3. กรอกข้อมูล: ชื่อ, นามสกุล, อีเมล, เบอร์โทร, วันเกิด, รหัสผ่าน
4. คลิก "สมัครสมาชิก"

### 2. เข้าสู่ระบบ
1. คลิกปุ่ม "เข้าสู่ระบบ"
2. คลิก "เข้าสู่ระบบด้วย Email"
3. กรอกอีเมลและรหัสผ่าน
4. คลิก "เข้าสู่ระบบ"

### 3. เพิ่มสินค้าลงตะกร้า
1. เลือกสินค้าที่ต้องการ
2. คลิกปุ่ม "เพิ่มลงตะกร้า"
3. ระบบจะบันทึกลงฐานข้อมูล (ต้อง Login ก่อน)

### 4. ดูตะกร้าสินค้า
1. คลิกไอคอนตะกร้าบน Navbar
2. จัดการจำนวนสินค้า
3. ดำเนินการสั่งซื้อ

---

## 🐛 Troubleshooting

### MongoDB Connection Error
```
❌ MongoDB connection error: connect ECONNREFUSED
```
**แก้ไข:** ตรวจสอบว่า MongoDB กำลังรันอยู่

```bash
# Windows
net start MongoDB
```

### Port Already in Use
```
Error: Port 5000 is already in use
```
**แก้ไข:** เปลี่ยน PORT ในไฟล์ `.env`

### CORS Error
**แก้ไข:** ตรวจสอบว่า `FRONTEND_URL` ในไฟล์ `.env` ถูกต้อง

---

## 📞 Support

หากพบปัญหาหรือมีคำถาม:
1. ตรวจสอบ Console ของ Browser และ Terminal
2. ตรวจสอบว่า MongoDB และ Backend กำลังรันอยู่
3. ตรวจสอบ Environment Variables

---

## 🎉 สิ่งที่เปลี่ยนจาก Mock Data

### ก่อนหน้า (Mock)
- ข้อมูลถูกเก็บใน localStorage
- ไม่มีการยืนยันตัวตนจริง
- ข้อมูลหายเมื่อลบ localStorage

### ตอนนี้ (Real Backend)
- ✅ ข้อมูลถูกเก็บใน MongoDB
- ✅ ระบบ Authentication จริงด้วย JWT
- ✅ ข้อมูลปลอดภัยและถาวร
- ✅ รองรับ Multi-user
- ✅ Admin System
- ✅ Order Management
- ✅ Stock Management

---

## 📚 Next Steps

1. เพิ่มระบบ Payment Gateway
2. เพิ่มระบบ Upload รูปภาพ
3. เพิ่มระบบ Email Notification
4. เพิ่มระบบ Review & Rating
5. เพิ่มระบบ Wishlist
6. Deploy to Production (Vercel + MongoDB Atlas)

---

**Happy Coding! 🚀**
